#include <stdio.h>

/**
*
* _islower - checks if a variable is lower cased
* Return: 0 if variable is not lower cased and 1 if variable is lower cased
* @c: variable to be passed in
*
*/

int _islower(char c)
{
	if (islower(c))
	{
		return (1);
	}
	else
	{

		return (0);
	{


}
