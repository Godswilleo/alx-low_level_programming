#include <stdio.h>

/**
* main - entry point
* Return: 0 or 1
*
* check_islower - checks if a variable is lower cased
* Return: 0 if variable is not lower cased and 1 if variable is lower cased
* @c: variable to be passed in
*
*/

int check_islower(char c)
{
	if (islower(c))
	{
		return (1);
	}
	else
	{

		return (0);
	{


}


int main(void)
{
	int r;

	r = _islower('H');
	putchar(r + '0');
	r = _islower('o');
	putchar(r + '0');
	r = _islower(108)
	putchar(r + '0');
	putchar('\n');
	return (0);


{
