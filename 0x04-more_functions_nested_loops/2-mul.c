#include <stdio.h>

/**
* mul - multiplies two digits
* Return: result
* @num1: first variable to be multiplied
* @num2: second variable to be multiplied
*/

int mul(int num1, int num2)
{
	int result;

	result = num1 * num2;
	return (result);

}
