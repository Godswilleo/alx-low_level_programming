#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{

	char *str;
	int len;

	a = 50;
	b = 80;

	str = "My first strlen";
	len = _stren(str)

	printf("%d\n", len);
	return (0);
}
