#include <stdio.h>
#include <stdlib.h>
#include <string.h>


/**
 * _strlen - checks the length of a string
 * Return: returns to string length
 * @s: string varaible to be checked
 *
 */

int _strlen(char *s)
{
	int r;

	r = strlen(s);
	return (r);
}
