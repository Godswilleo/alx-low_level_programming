#include <string.h>
#include <stdio.h>
/**
 * _strchr - locates a character in a string
 * Return: string
 * @s: string variable
 * @c: character variable to locate
 */

char *_strchr(char *s, char c)
{
	char *p = strchr(s, c)

	return (p);
}
