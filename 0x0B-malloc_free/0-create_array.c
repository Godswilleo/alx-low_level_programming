#include <stdio.h>

/**
* create_array - creates an array and returns a pointer
*
*/

char *create_array(unsigned int size, char c)
{


char *i = malloc(sizeof(c), * size)
	if (i == NULL)
	{
		return (NULL);
	}


}
